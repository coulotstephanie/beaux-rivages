import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";import{housekeepingActionSchema}from"../platform/housekeeping/schemas";
const uuid="53ef6a99-9a31-4557-bd7b-64de7beb7620";
test("les checklists tactiles sont validées strictement",()=>{assert.equal(housekeepingActionSchema.safeParse({action:"update_task",taskId:uuid,operationalStatus:"ready",offlineRevision:2,checklist:[{id:"wifi",label:"Connexion Wi-Fi",done:true}]}).success,true);assert.equal(housekeepingActionSchema.safeParse({action:"update_task",taskId:uuid,operationalStatus:"unknown",offlineRevision:-1,checklist:[]}).success,false);});
test("la synchronisation hors ligne utilise un verrou optimiste",()=>{const repository=readFileSync("platform/housekeeping/repository.ts","utf8");assert.ok(repository.includes('.eq("offline_revision",input.offlineRevision)'));const ui=readFileSync("components/admin/HousekeepingAdmin.tsx","utf8");assert.match(ui,/br-housekeeping-queue/);assert.ok(ui.includes('addEventListener("online"'));});
test("les photos opérationnelles sont privées et limitées",()=>{const sql=readFileSync("supabase/migrations/20260729143000_housekeeping_maintenance_premium.sql","utf8");assert.ok(sql.includes("values('operations','operations',false,10485760"));assert.ok(sql.includes("image/jpeg"));assert.match(sql,/staff manage operational photos/);});
test("chaque maison possède sa checklist métier",()=>{const sql=readFileSync("supabase/migrations/20260729143000_housekeeping_maintenance_premium.sql","utf8");for(const slug of["chai-des-tortues","villa-raie-manta","nid-d-ete"])assert.match(sql,new RegExp(slug));for(const item of["Air Fryer Ninja","Barbecue : cendres","Portail et télécommande"])assert.match(sql,new RegExp(item));});
test("l’API housekeeping protège lecture et mutations",()=>{const route=readFileSync("app/api/admin/housekeeping/route.ts","utf8");assert.match(route,/requireAdmin/);assert.match(route,/requireSameOrigin/);assert.match(route,/housekeepingActionSchema/);});
