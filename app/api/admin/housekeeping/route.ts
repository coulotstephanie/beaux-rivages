import{NextRequest}from"next/server";import{isDatabaseConfigured}from"@/platform/database/client";import{HousekeepingRepository}from"@/platform/housekeeping/repository";import{housekeepingActionSchema}from"@/platform/housekeeping/schemas";import{noStoreJson,rateLimit,requireAdmin,requireSameOrigin}from"@/platform/http/security";
export async function GET(r:NextRequest){const l=rateLimit(r,20);if(l)return l;if(!requireAdmin(r))return noStoreJson({error:"Authentification requise."},{status:401});if(!isDatabaseConfigured())return noStoreJson({error:"Base non configurée."},{status:503});try{return noStoreJson(await new HousekeepingRepository().snapshot());}catch(e){return noStoreJson({error:"Housekeeping indisponible.",code:e instanceof Error?e.message.split(":")[0]:"UNKNOWN"},{status:500});}}
export async function POST(r:NextRequest){const l=rateLimit(r,15);if(l)return l;if(!requireSameOrigin(r))return noStoreJson({error:"Origine non autorisée."},{status:403});if(!requireAdmin(r))return noStoreJson({error:"Authentification requise."},{status:401});const p=housekeepingActionSchema.safeParse(await r.json().catch(()=>null));if(!p.success)return noStoreJson({error:"Données invalides.",details:p.error.flatten()},{status:400});try{return noStoreJson({ok:true,result:await new HousekeepingRepository().execute(p.data)},{status:201});}catch(e){const code=e instanceof Error?e.message.split(":")[0]:"UNKNOWN";return noStoreJson({error:code==="OFFLINE_CONFLICT"?"Cette checklist a été modifiée sur un autre appareil. Actualisez avant de reprendre.":"Action impossible.",code},{status:code==="OFFLINE_CONFLICT"?409:500});}}
